{% extends '::base.html.twig' %}

{% block title %}
Edit Registry Key
{% endblock %}

{% block body %}
<h1>Edit Registry Key</h1>

{{ form_start(form) }}
    <fieldset class="control-group">
        {{ form_rest(form) }}
    </fieldset>
    <div class="form-group">
        <button type="submit" class="btn btn-primary">Save</button> <a href="{{ back_url }}" class="btn">Cancel</a>
    </div>
{{ form_end(form) }}
{% endblock %}
